{
  "name": "droptrack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server/index.js",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.0",
    "dotenv": "^16.0.0"
  }
}
